<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Météose homepage</title>
</head>
<body>
    <div class="container">
        <header>
          <div class="compass-icon"><img src="" alt=""></div>
        </header>
        <div class="weather-title">
          <h1>Meteose</h1>
          <p>meteose</p>
        </div>
        <form action="/" method="POST">
          <input type="text" name="city" class="ghost-input" placeholder="Enter a city">
        </form>
    
        <div class="result">
          <% if(temp !== null)  {%>
            <h1 class="temp"><%= temp %></h1>
          <% } %>
    
          <div class="icon-des">
            <% if(icon !== null)  {%>
              <img class="icon" src="" alt="">
            <% } %>
            <% if(des !== null)  {%>
              <h3><%= des %></h3>
            <% } %>
          </div>
    
          <% if(city !== null)  {%>
            <p class="city"><%= city %></p>
          <% } %>
          
        </div>
</body>
</html>